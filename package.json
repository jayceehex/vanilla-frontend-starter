{
  "name": "vanilla-frontend-starter",
  "version": "0.0.1",
  "description": "My personal template for developing quick, small front-end projects.",
  "scripts": {
    "build": "npm run clean && parcel build src/index.html",
    "clean": "rm -rf ./dist",
    "develop": "parcel src/index.html --open",
    "lint:check": "npx prettier src/ --check",
    "lint:write": "npx prettier src/ --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JC Hoskins",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0",
    "prettier": "2.7.1",
    "sass": "^1.54.9"
  }
}
